TranslationConfig = {
  --- Language
  ---
  --- The language used for every radio translation: the Lua strings
  --- (keybind labels, commands, notifications, console) and the interface,
  --- whose strings are compiled from web/messages/<language>.json.
  ---
  --- Available: 'en', 'fr'
  ---
  --- Default: 'fr'
  language = 'fr',
}
